{% extends "base.html.twig" %}

{% block title %}
    List of Models for
{% endblock %}

{% block body %}

   <h2>List of Models for make: {{ make.name}}</h2>

    {% for m in model  %}
        <p><a href="{{ path('year_list',{'slug':m.slug}) }}">{{ m.name }}</a></p>
    {% endfor %}

{% endblock %}
