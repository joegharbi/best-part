{% extends "base.html.twig" %}

{% block title %}
    List of years for
{% endblock %}

{% block body %}


   <h2>List of years for make: </h2>


    {% for  m in model  %}

        <p><a href="{{ path('car', {'slug': m.slug}) }}">{{ m.name }}</a></p>

    {% endfor %}

{#    {{ form_start(formView) }}#}

{#    {{ form_widget(formView) }}#}
{#    <button class="btn submit btn-success">#}
{#        Search#}
{#    </button>#}
{#    {{ form_end(formView) }}#}

{% endblock %}
